manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned to a known-good commit (2025-06-16) instead of tracking `main`.
      # Tracking `main` broke the build after ZMK moved to Zephyr 4.1 and a new
      # board/hardware model ("No board named 'nice_nano_v2' found").
      revision: 6b44d33db2f4bad7d98e475e6f7968493b05af73
      import: app/west.yml
  self:
    path: config
